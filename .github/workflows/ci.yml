name: CI

# PR 门禁。push 到 main 的同一套检查由 docker-staging.yml 的 checks job 承担
# （staging 镜像构建挂在其后），故此处不再 on push:main，避免同一次 push 重复跑。
on:
  pull_request:

jobs:
  checks:
    uses: ./.github/workflows/checks.yml
